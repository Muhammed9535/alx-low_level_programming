#include "main.h"

/**
 * _abs - compute tthe absolute value of an integer.
 * @n: The interger to be computed
 *
 * Return: The absolute value of tthe integerr.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
