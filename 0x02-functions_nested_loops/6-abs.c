#include "main.h"
/**
  * _abs - a function that computes the absolute value of an integer.
  * @x: character to print
  * Return: 0
  */
int _abs(int x)
{
	if (x < 0)
	{
		x *= -1;
		return (x);
	}
	else
	{
		return (x);
	}
}
